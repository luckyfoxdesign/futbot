services:
  bot:
    build: .
    container_name: c-futbot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - internal_net

networks:
  internal_net:
    external: true
